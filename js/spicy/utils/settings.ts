// Compatibility stub — Spicy's settings panel is a React tree that is not
// ported. The view-controls button for it is hidden by Monochrome-side CSS
// until that port happens. All behaviour settings keep their defaults from
// the (adapted) stores.
export function openSettingsPanel(): void {}
